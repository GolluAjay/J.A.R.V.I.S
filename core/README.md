Legacy `core/` Python modules moved to **`src/jarvis/`** (installable package).

Use:

- `pip install -e .` from the repo root, or
- `export PYTHONPATH=/path/to/JARVIS/src`

Then `import jarvis` or run `./jarvis hud`.
